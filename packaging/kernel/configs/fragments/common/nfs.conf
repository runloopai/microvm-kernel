# NFSv3 client for shared Devbox workspace mounts.
# The volume gateway serves NFS and mountd over fixed TCP ports; UDP, NFSv2,
# NFSv4, and server support are not needed in the guest.
CONFIG_NFS_FS=y
CONFIG_NFS_V3=y
CONFIG_NFS_DISABLE_UDP_SUPPORT=y
